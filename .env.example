# Server Configuration
PORT=8080
GIN_MODE=debug  # Use 'release' in production

# Database Configuration
DB_HOST=db
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=launch_scheduler
DB_PORT=5432

# Optional: JWT Configuration (for future auth implementation)
JWT_SECRET_KEY=your_jwt_secret_key
JWT_EXPIRATION_HOURS=24

# Optional: SpaceX API Configuration (for future integration)
SPACEX_API_URL=https://api.spacexdata.com/v4 